class test_simple
{
    function application_start()
    {
        num:x = 1;
        num:y = 10;
        num:z = x + y;

        testIf(x,y,z);
    }

    function testIf(num:x, num:y, num:z):num {
        if (x < y < z) {
            return 1;
        } else {
            return 0;
        }
    }
}

